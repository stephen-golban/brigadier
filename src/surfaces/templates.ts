// biome-ignore-all lint/suspicious/noTemplateCurlyInString: `${CLAUDE_PLUGIN_ROOT}` and `${__dirname}` are literal bytes of a Claude Code hook definition and an .mcpb manifest. The hosts expand them; this file must not.

/**
 * Every host-side template, compiled into the binary.
 *
 * THIS FILE IS A COPY. The canonical, reviewable assets live in `surfaces/` at
 * the repository root, and `test/surfaces.test.ts` fails the build if this file
 * and that directory disagree by a single byte, in either direction, for any
 * file. Edit `surfaces/`, then bring this file to match; never the reverse, and
 * never only one of them.
 *
 * THE COPY IS NOT AVOIDABLE, and it is worth saying why rather than leaving a
 * reader to assume it is sloppiness. brigadier ships as ONE FILE: a native
 * executable produced by `bun build --compile`. A user who installed it with a
 * single `curl` has no `surfaces/` directory on their machine, and
 * `brigadier install` still has to be able to write the doctrine. So the bytes
 * have to be inside the binary. Keeping them ALSO as real files is what makes
 * them reviewable in a diff, greppable, and testable against the thing that
 * ships — and the equality test is what keeps the two from becoming two
 * different products.
 *
 * `surfaces/README.md` is deliberately absent from this map: it documents the
 * directory for a reader of the repository and is never installed anywhere.
 *
 * Keys are paths relative to `surfaces/`. `src/surfaces/install.ts` maps each
 * one to its destination on the user's machine.
 */

export const SURFACE_TEMPLATES: Readonly<Record<string, string>> =
  Object.freeze({
    "claude-code/.claude-plugin/plugin.json":
      '{\n  "name": "brigadier",\n  "description": "You are not the worker: hand coding work to the brigadier CLI.",\n  "author": {\n    "name": "brigadier"\n  }\n}\n',
    "claude-code/SKILL.md":
      '---\nname: brigadier\ndescription: Load before starting any coding task with more than one independent piece of work — a feature, a cross-file refactor, a migration, a bug with several call sites, a batch of mechanical edits. Routes the work to the brigadier CLI instead of doing it in this session.\n---\n\n# You are not the worker\n\nThis machine has `brigadier` installed. When a task decomposes into independent\npieces, you do not write them. You plan them, hand them to `brigadier run`, and\nreview what comes back. Your context is for coordination and judgement; a worker\nsession is disposable and starts clean.\n\n## What to do\n\n1. **Decompose.** Two slices are independent when no file is owned by both.\n   Partition by file ownership, not by topic.\n2. **Write a plan document** (JSON):\n\n   ```json\n   {\n     "id": "add-retry",\n     "goal": "Retry transient HTTP failures in the client",\n     "slices": [\n       {\n         "id": "retry-core",\n         "title": "Retry loop and backoff",\n         "prompt": "One self-contained brief: the interfaces, the constraints, the definition of done, and the evidence required.",\n         "ownedPaths": ["src/http/retry.ts"],\n         "difficulty": "standard"\n       }\n     ]\n   }\n   ```\n\n   `id`, `title`, `prompt`, `ownedPaths` and `difficulty` are required on every\n   slice. `difficulty` is one of `routine`, `standard`, `hard`, and it has no\n   default: a plan that does not say how hard a slice is has not been planned.\n   `dependsOn` is parsed but **not usable yet**: a dependent slice would not see\n   its prerequisite\'s output, so `brigadier run` refuses any plan that declares\n   one. Split dependent work into separate runs. `requires` is optional and takes\n   `imageInput`, `webSearch`, `structuredOutput`, and `minContextWindowTokens`.\n3. **Dry-run it first.** `brigadier run --plan plan.json --dry-run` routes every\n   slice and prints which vendor, model and effort would take it, without\n   creating a worktree or spawning anything. A slice that cannot route is worth\n   knowing about before a worker is spent on its siblings.\n4. **Run it.** `brigadier run --plan plan.json --max-workers 3`. Each slice gets\n   its own git worktree and its own branch; what lands is merged onto one\n   integration branch.\n5. **Review the diffs.** You own the outcome. A worker\'s report is a claim, not\n   evidence.\n\n## What not to do\n\n- Do not edit a file while a slice that owns it is running.\n- Do not do the work yourself because it "looks quick". A one-file change is a\n  one-slice plan, and writing the plan costs less than the context you spend\n  doing it here.\n- Do not invent commands. brigadier has exactly two: `init` and `run`.\n  `brigadier run "fix the parser"` is **not implemented** — a bare task\n  description is refused, and `--plan <file>` (or `--plan -` for stdin) is\n  required.\n\n## Before the first run on a machine\n\n`brigadier init` scans for installed worker CLIs and writes\n`$BRIGADIER_HOME/config.json`, defaulting to `~/.brigadier/config.json`. Without\nit `brigadier run` exits 1 and tells you to run `init`.\n\n## Exit codes worth branching on\n\n`0` succeeded · `1` never started (no config, bad plan file, missing HOME/PATH/USER)\n· `2` usage error · `3` the run started and did not succeed · `130`/`143`\ninterrupted.\n',
    "claude-code/hooks/README.md":
      '# The handoff hook on Claude Code\n\n**Status: works, with no click and no trust prompt.**\n\n`PreCompact` fires when the session is about to compact — which is to say, when\nit has run out of context. That is the exact moment the doctrine is most needed\nand least likely to be followed, so it is where the hook lives. The event carries\n`transcript_path`, which is what makes the hook *transcript-watching* rather than\nmerely periodic: it reports how many assistant turns were spent before the\nhandoff.\n\n`hooks.json` registers `handoff.mjs` against `PreCompact` with matcher `*`.\n`${CLAUDE_PLUGIN_ROOT}` is expanded by Claude Code to the installed skill\ndirectory, so the registration survives being installed anywhere.\n\n## The contract `handoff.mjs` implements\n\n```\nin    one JSON object on stdin, optionally carrying `transcript_path`\nout   one JSON object on stdout: {"systemMessage": "<one sentence>"}\nexit  always 0\n```\n\nNothing about that contract is Claude-Code-specific, which is why the identical\nfile is shipped under `surfaces/codex/hooks/`. A host that wants a different\npayload shape needs one line changed, not a rewrite.\n\n## What it deliberately does not do\n\n- It does not block, veto, or rewrite anything. It says one sentence.\n- It does not exit non-zero, ever. A hook that fails, fails inside somebody\n  else\'s session.\n- It does not walk a transcript larger than 8 MB, and it does not wait longer\n  than 2 seconds for stdin to end. Both degrade to the shorter message rather\n  than to a stall.\n\n## Where this hook does not exist\n\n- **Codex** — trust-gated. See `surfaces/codex/hooks/README.md`.\n- **Claude Desktop** — impossible. Desktop exposes no hook surface at all.\n- **opencode** — provided instead by `surfaces/opencode/plugin/brigadier.js`,\n  because opencode\'s seam is the plugin event bus rather than a hook process.\n',
    "claude-code/hooks/handoff.mjs":
      '#!/usr/bin/env node\n\n/**\n * The handoff hook (design decision #10).\n *\n * A host session that is about to run out of context is exactly the session most\n * tempted to keep grinding rather than delegate. This hook watches for that\n * moment and says the one sentence the doctrine exists to say: stop, write a\n * plan, hand it to `brigadier run`, and keep the review.\n *\n * THE CONTRACT, which is deliberately host-agnostic:\n *   in   one JSON object on stdin, optionally carrying `transcript_path`\n *   out  one JSON object on stdout: {"systemMessage": "<one sentence>"}\n *   exit always 0\n *\n * A hook that fails is worse than a hook that is absent, because it fails inside\n * somebody else\'s session. So every unhappy path here — unparseable stdin, a\n * missing transcript, an unreadable transcript, a transcript too large to walk —\n * degrades to a weaker message or to silence, and never to a non-zero exit.\n *\n * Zero dependencies, node: builtins only. It is copied verbatim into each host\'s\n * configuration directory by `brigadier install`, so it cannot import anything\n * from the brigadier tree either.\n */\n\nimport { readFileSync, statSync } from "node:fs";\n\n/** Beyond this, the transcript is not walked: a hook must not stall a session. */\nconst MAX_TRANSCRIPT_BYTES = 8_000_000;\n\n/** A hook whose stdin never ends would wedge the host. This is the escape. */\nconst DEFAULT_STDIN_TIMEOUT_MS = 2000;\n\nconst ADVICE =\n  "Do not carry the remaining work here — write it as a brigadier plan and run `brigadier run --plan <file>`. A fresh worker gets a clean context; you keep the review.";\n\nmain();\n\nfunction main() {\n  readStdin((raw) => {\n    const message = buildMessage(raw);\n    if (message !== null) {\n      process.stdout.write(`${JSON.stringify({ systemMessage: message })}\\n`);\n    }\n    process.exitCode = 0;\n  });\n}\n\n/**\n * Null means "say nothing", which is the right answer for input this hook does\n * not understand. Silence is always a legal hook response; a guess is not.\n */\nfunction buildMessage(raw) {\n  let payload;\n  try {\n    payload = JSON.parse(raw);\n  } catch {\n    return null;\n  }\n  if (typeof payload !== "object" || payload === null) {\n    return null;\n  }\n  const turns = countAssistantTurns(payload.transcript_path);\n  return turns === null\n    ? `brigadier: this session is out of context. ${ADVICE}`\n    : `brigadier: this session is out of context after ${turns} assistant turn(s). ${ADVICE}`;\n}\n\n/** Null whenever the count cannot be established honestly. */\nfunction countAssistantTurns(transcriptPath) {\n  if (typeof transcriptPath !== "string" || transcriptPath.length === 0) {\n    return null;\n  }\n  try {\n    if (statSync(transcriptPath).size > MAX_TRANSCRIPT_BYTES) {\n      return null;\n    }\n    let turns = 0;\n    for (const line of readFileSync(transcriptPath, "utf8").split("\\n")) {\n      if (line.length === 0) {\n        continue;\n      }\n      try {\n        if (JSON.parse(line).type === "assistant") {\n          turns += 1;\n        }\n      } catch {\n        // A partially written transcript line is normal; skip it.\n      }\n    }\n    return turns;\n  } catch {\n    return null;\n  }\n}\n\n/**\n * Drains stdin, or gives up on it.\n *\n * THE TEARDOWN IS THE POINT, not the timeout. A host that writes the hook\'s\n * payload and then forgets to close the pipe leaves this process holding an open\n * read handle, and an open read handle keeps the event loop alive forever — so a\n * timeout that only *fired* would print the message and then hang anyway, which\n * is the exact failure the timeout existed to prevent. `finish` therefore stops\n * listening and releases the stream before it answers.\n */\nfunction readStdin(done) {\n  const timeoutMs = readTimeout();\n  let text = "";\n  let finished = false;\n  const onData = (chunk) => {\n    text += chunk;\n  };\n  const finish = () => {\n    if (finished) {\n      return;\n    }\n    finished = true;\n    clearTimeout(timer);\n    process.stdin.removeListener("data", onData);\n    process.stdin.pause();\n    process.stdin.unref?.();\n    done(text);\n  };\n  const timer = setTimeout(finish, timeoutMs);\n  timer.unref?.();\n  process.stdin.setEncoding("utf8");\n  process.stdin.on("data", onData);\n  process.stdin.on("end", finish);\n  process.stdin.on("error", finish);\n}\n\nfunction readTimeout() {\n  const raw = process.env.BRIGADIER_HANDOFF_STDIN_TIMEOUT_MS;\n  if (raw === undefined || !/^[0-9]+$/.test(raw)) {\n    return DEFAULT_STDIN_TIMEOUT_MS;\n  }\n  const parsed = Number(raw);\n  return Number.isSafeInteger(parsed) && parsed > 0\n    ? parsed\n    : DEFAULT_STDIN_TIMEOUT_MS;\n}\n',
    "claude-code/hooks/hooks.json":
      '{\n  "hooks": {\n    "PreCompact": [\n      {\n        "matcher": "*",\n        "hooks": [\n          {\n            "type": "command",\n            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/handoff.mjs"\n          }\n        ]\n      }\n    ]\n  }\n}\n',
    "claude-desktop/README.md":
      "# The Claude Desktop surface\n\n**Status: MCP server. No skill, and no handoff hook — neither is possible here.**\n\n## Why Desktop gets a different artifact from every other host\n\nDesktop **Skills execute server-side**. They run in Anthropic's infrastructure,\nnot on your laptop, so a skill cannot invoke `brigadier`, cannot see your\nrepository, and cannot spawn `claude -p`. Installing the doctrine as a Desktop\nskill would produce a model that has been told to delegate and has nothing to\ndelegate to.\n\nDesktop **MCP servers run locally, with your full user privileges**. They can\nspawn `claude -p`, read your working tree, and start a real run. That asymmetry\nis the entire reason brigadier ships an MCP server (`src/mcp/server.ts`), and the\nreason Desktop is the one host where the tiny doctrine file is not the answer.\n\n## The handoff hook does not exist here\n\nDesign decision #10's transcript-watching handoff works on Claude Code, works on\nopencode, and is trust-gated on Codex. On Desktop it is **impossible**, not\nmerely unimplemented: Desktop exposes no hook surface of any kind, and an MCP\nserver is invoked by the model when the model chooses to invoke it — never by the\ntranscript, and never at the moment the context fills. There is no workaround and\nnone is offered.\n\n## Installing\n\n`brigadier install claude-desktop` stages this directory to\n`~/.brigadier/surfaces/claude-desktop/` and prints what to do next. It does not\ninstall anything into Desktop itself, because Desktop installs a bundle by an\nexplicit user action and brigadier does not forge those.\n\nTo produce the installable `.mcpb`:\n\n1. `bun run build:mcp` — emits the server next to this manifest at\n   `server/brigadier-mcp.js`.\n2. Zip the staged directory with `manifest.json` at the archive root and rename\n   it `brigadier.mcpb`.\n3. Open it with Claude Desktop, or drop it into Desktop's extensions pane.\n\nThe server is zero-dependency: it speaks JSON-RPC 2.0 over stdio against the MCP\nspec directly, with no SDK. There is nothing to `npm install` inside the bundle.\n\n## The tool surface\n\nThree tools, and they are exactly what brigadier can do today — no more:\n\n| Tool | Effect |\n| --- | --- |\n| `brigadier_validate_plan` | Pure. Shape defects, scheduling defects, and the dependency waves a plan would run in. |\n| `brigadier_route_plan` | Reads this machine's config. Reports the vendor, model, and effort each slice would get, or why nothing can take it. Creates no worktree, spawns no worker. |\n| `brigadier_run` | The real thing: worktrees, workers, commits, one integration branch. |\n\nThe `tools` array in `manifest.json` must name exactly these three;\n`test/mcp.test.ts` fails if the manifest and the server ever disagree.\n",
    "claude-desktop/manifest.json":
      '{\n  "manifest_version": "0.2",\n  "name": "brigadier",\n  "display_name": "brigadier",\n  "version": "0.0.0",\n  "description": "Hand coding work to the brigadier CLI running locally on this machine.",\n  "long_description": "Claude Desktop Skills execute server-side and cannot invoke a binary on your laptop. Desktop MCP servers run locally with full user privileges and can spawn `claude -p`. That is why brigadier reaches Desktop as an MCP server rather than as a skill: it is the only surface that can actually start a run.",\n  "author": {\n    "name": "brigadier"\n  },\n  "server": {\n    "type": "node",\n    "entry_point": "server/brigadier-mcp.js",\n    "mcp_config": {\n      "command": "node",\n      "args": ["${__dirname}/server/brigadier-mcp.js"]\n    }\n  },\n  "tools": [\n    {\n      "name": "brigadier_validate_plan",\n      "description": "Check a brigadier plan document for shape defects and scheduling defects, and report the dependency waves it would run in. Touches nothing."\n    },\n    {\n      "name": "brigadier_route_plan",\n      "description": "Report which vendor, model, and effort this machine would give each slice of a plan, or why no model can take it. Creates no worktree and spawns no worker."\n    },\n    {\n      "name": "brigadier_run",\n      "description": "Run a brigadier plan on a local repository: route every slice, spawn a worker for each, commit what it produced, and merge the lot."\n    }\n  ],\n  "tools_generated": false,\n  "keywords": ["brigadier", "orchestration", "delegation", "coding"],\n  "compatibility": {\n    "runtimes": {\n      "node": ">=20.0.0"\n    }\n  }\n}\n',
    "codex/AGENTS.md":
      '# You are not the worker\n\nThis machine has `brigadier` installed. When a coding task decomposes into\nindependent pieces, do not write them here. Plan them, hand them to\n`brigadier run`, and review what comes back. Your context is for coordination\nand judgement; a worker session is disposable and starts clean.\n\n1. **Decompose by file ownership.** Two slices are independent when no file is\n   owned by both.\n2. **Write a plan document** (JSON). Every slice needs `id`, `title`, `prompt`,\n   `ownedPaths`, and `difficulty` (`routine`, `standard`, or `hard` — there is no\n   default). `requires` is optional. `dependsOn` is parsed but **not usable\n   yet** — a dependent slice would not see its prerequisite\'s output, so a plan\n   declaring one is refused; split dependent work into separate runs.\n\n   ```json\n   {\n     "id": "add-retry",\n     "goal": "Retry transient HTTP failures in the client",\n     "slices": [\n       {\n         "id": "retry-core",\n         "title": "Retry loop and backoff",\n         "prompt": "One self-contained brief: interfaces, constraints, definition of done, evidence required.",\n         "ownedPaths": ["src/http/retry.ts"],\n         "difficulty": "standard"\n       }\n     ]\n   }\n   ```\n\n3. **Dry-run first:** `brigadier run --plan plan.json --dry-run` routes every\n   slice and prints which vendor, model, and effort would take it, creating no\n   worktree and spawning nothing.\n4. **Run it:** `brigadier run --plan plan.json --max-workers 3`. Each slice gets\n   its own worktree and branch; what lands is merged onto one integration branch.\n5. **Review the diffs yourself.** A worker\'s report is a claim, not evidence.\n\n## Do not\n\n- Edit a file while a slice that owns it is running.\n- Do the work here because it "looks quick" — a one-file change is a one-slice\n  plan.\n- Invent commands. brigadier has exactly two: `init` and `run`. A bare\n  `brigadier run "fix the parser"` is refused; `--plan <file>` (or `--plan -`) is\n  required.\n\n`brigadier init` must have been run once on this machine; it writes\n`$BRIGADIER_HOME/config.json`, defaulting to `~/.brigadier/config.json`.\n\n## A note specific to Codex\n\nbrigadier drives `codex` as a plain subprocess and passes\n`project_doc_max_bytes=0` or `--ignore-user-config` when a slice asks for\ninstruction isolation. Codex 0.145.0 loads `$CODEX_HOME/AGENTS.md` — this file —\nregardless of either flag, and exposes no way to suppress it. So this file is\nread by brigadier\'s own workers too. Keep it short, keep it doctrine, and put\nnothing in it that a worker running one slice should not see.\n',
    "codex/hooks/README.md":
      '# The handoff hook on Codex\n\n**Status: trust-gated. It is a click, and the click comes back.**\n\nCodex will not run a hook it has not been told to trust. Approving one is a\nmanual, interactive click. The hook *definition is hashed*, so the approval is\nbound to the exact bytes of what you approved: edit `handoff.mjs` by one\ncharacter and Codex asks again. That is a deliberate safety property of Codex and\nbrigadier neither works around it nor pretends it is not there. If you want the\nhandoff hook on Codex, you are signing up for a click now and another click after\nevery upgrade of brigadier that touches this file.\n\nOn Claude Code the same hook installs with no click at all. That asymmetry is\nreal, and it is the reason this file exists rather than a line in a table.\n\n## The contract `handoff.mjs` implements\n\n```\nin    one JSON object on stdin, optionally carrying `transcript_path`\nout   one JSON object on stdout: {"systemMessage": "<one sentence>"}\nexit  always 0\n```\n\nThis file is byte-for-byte identical to\n`surfaces/claude-code/hooks/handoff.mjs`, and a test enforces that. The payload\nshape above is Claude Code\'s. If your Codex version wants a different response\nshape, the change is the one `JSON.stringify` call at the top of `main`.\n\n## Registering it — the part brigadier does not do for you\n\n`brigadier install codex` copies this script to\n`~/.agents/skills/brigadier/hooks/handoff.mjs` and stops there. **It does not\nwrite a Codex hook registration, on purpose.** The configuration key and the\nevent name differ across Codex releases, and brigadier\'s Codex support is pinned\nto CLI 0.145.0 for the *worker* protocol only — the hook surface was not verified\nagainst a running Codex. Writing a guessed key into your `config.toml` would be a\nsilent no-op at best.\n\nTake the event name and the registration syntax from the hook documentation for\nthe Codex version you have installed, point it at the path above, and click\nthrough the trust prompt. If your Codex has no hook surface, you have the skill\nand `AGENTS.md`, which carry the same doctrine without the handoff trigger.\n',
    "codex/hooks/handoff.mjs":
      '#!/usr/bin/env node\n\n/**\n * The handoff hook (design decision #10).\n *\n * A host session that is about to run out of context is exactly the session most\n * tempted to keep grinding rather than delegate. This hook watches for that\n * moment and says the one sentence the doctrine exists to say: stop, write a\n * plan, hand it to `brigadier run`, and keep the review.\n *\n * THE CONTRACT, which is deliberately host-agnostic:\n *   in   one JSON object on stdin, optionally carrying `transcript_path`\n *   out  one JSON object on stdout: {"systemMessage": "<one sentence>"}\n *   exit always 0\n *\n * A hook that fails is worse than a hook that is absent, because it fails inside\n * somebody else\'s session. So every unhappy path here — unparseable stdin, a\n * missing transcript, an unreadable transcript, a transcript too large to walk —\n * degrades to a weaker message or to silence, and never to a non-zero exit.\n *\n * Zero dependencies, node: builtins only. It is copied verbatim into each host\'s\n * configuration directory by `brigadier install`, so it cannot import anything\n * from the brigadier tree either.\n */\n\nimport { readFileSync, statSync } from "node:fs";\n\n/** Beyond this, the transcript is not walked: a hook must not stall a session. */\nconst MAX_TRANSCRIPT_BYTES = 8_000_000;\n\n/** A hook whose stdin never ends would wedge the host. This is the escape. */\nconst DEFAULT_STDIN_TIMEOUT_MS = 2000;\n\nconst ADVICE =\n  "Do not carry the remaining work here — write it as a brigadier plan and run `brigadier run --plan <file>`. A fresh worker gets a clean context; you keep the review.";\n\nmain();\n\nfunction main() {\n  readStdin((raw) => {\n    const message = buildMessage(raw);\n    if (message !== null) {\n      process.stdout.write(`${JSON.stringify({ systemMessage: message })}\\n`);\n    }\n    process.exitCode = 0;\n  });\n}\n\n/**\n * Null means "say nothing", which is the right answer for input this hook does\n * not understand. Silence is always a legal hook response; a guess is not.\n */\nfunction buildMessage(raw) {\n  let payload;\n  try {\n    payload = JSON.parse(raw);\n  } catch {\n    return null;\n  }\n  if (typeof payload !== "object" || payload === null) {\n    return null;\n  }\n  const turns = countAssistantTurns(payload.transcript_path);\n  return turns === null\n    ? `brigadier: this session is out of context. ${ADVICE}`\n    : `brigadier: this session is out of context after ${turns} assistant turn(s). ${ADVICE}`;\n}\n\n/** Null whenever the count cannot be established honestly. */\nfunction countAssistantTurns(transcriptPath) {\n  if (typeof transcriptPath !== "string" || transcriptPath.length === 0) {\n    return null;\n  }\n  try {\n    if (statSync(transcriptPath).size > MAX_TRANSCRIPT_BYTES) {\n      return null;\n    }\n    let turns = 0;\n    for (const line of readFileSync(transcriptPath, "utf8").split("\\n")) {\n      if (line.length === 0) {\n        continue;\n      }\n      try {\n        if (JSON.parse(line).type === "assistant") {\n          turns += 1;\n        }\n      } catch {\n        // A partially written transcript line is normal; skip it.\n      }\n    }\n    return turns;\n  } catch {\n    return null;\n  }\n}\n\n/**\n * Drains stdin, or gives up on it.\n *\n * THE TEARDOWN IS THE POINT, not the timeout. A host that writes the hook\'s\n * payload and then forgets to close the pipe leaves this process holding an open\n * read handle, and an open read handle keeps the event loop alive forever — so a\n * timeout that only *fired* would print the message and then hang anyway, which\n * is the exact failure the timeout existed to prevent. `finish` therefore stops\n * listening and releases the stream before it answers.\n */\nfunction readStdin(done) {\n  const timeoutMs = readTimeout();\n  let text = "";\n  let finished = false;\n  const onData = (chunk) => {\n    text += chunk;\n  };\n  const finish = () => {\n    if (finished) {\n      return;\n    }\n    finished = true;\n    clearTimeout(timer);\n    process.stdin.removeListener("data", onData);\n    process.stdin.pause();\n    process.stdin.unref?.();\n    done(text);\n  };\n  const timer = setTimeout(finish, timeoutMs);\n  timer.unref?.();\n  process.stdin.setEncoding("utf8");\n  process.stdin.on("data", onData);\n  process.stdin.on("end", finish);\n  process.stdin.on("error", finish);\n}\n\nfunction readTimeout() {\n  const raw = process.env.BRIGADIER_HANDOFF_STDIN_TIMEOUT_MS;\n  if (raw === undefined || !/^[0-9]+$/.test(raw)) {\n    return DEFAULT_STDIN_TIMEOUT_MS;\n  }\n  const parsed = Number(raw);\n  return Number.isSafeInteger(parsed) && parsed > 0\n    ? parsed\n    : DEFAULT_STDIN_TIMEOUT_MS;\n}\n',
    "codex/skills/brigadier/SKILL.md":
      '---\nname: brigadier\ndescription: Load before starting any coding task with more than one independent piece of work — a feature, a cross-file refactor, a migration, a bug with several call sites, a batch of mechanical edits. Routes the work to the brigadier CLI instead of doing it in this session.\n---\n\n# You are not the worker\n\nThis machine has `brigadier` installed. When a task decomposes into independent\npieces, you do not write them. You plan them, hand them to `brigadier run`, and\nreview what comes back. Your context is for coordination and judgement; a worker\nsession is disposable and starts clean.\n\n## What to do\n\n1. **Decompose.** Two slices are independent when no file is owned by both.\n   Partition by file ownership, not by topic.\n2. **Write a plan document** (JSON):\n\n   ```json\n   {\n     "id": "add-retry",\n     "goal": "Retry transient HTTP failures in the client",\n     "slices": [\n       {\n         "id": "retry-core",\n         "title": "Retry loop and backoff",\n         "prompt": "One self-contained brief: the interfaces, the constraints, the definition of done, and the evidence required.",\n         "ownedPaths": ["src/http/retry.ts"],\n         "difficulty": "standard"\n       }\n     ]\n   }\n   ```\n\n   `id`, `title`, `prompt`, `ownedPaths` and `difficulty` are required on every\n   slice. `difficulty` is one of `routine`, `standard`, `hard`, and it has no\n   default: a plan that does not say how hard a slice is has not been planned.\n   `dependsOn` is parsed but **not usable yet**: a dependent slice would not see\n   its prerequisite\'s output, so `brigadier run` refuses any plan that declares\n   one. Split dependent work into separate runs. `requires` is optional and takes\n   `imageInput`, `webSearch`, `structuredOutput`, and `minContextWindowTokens`.\n3. **Dry-run it first.** `brigadier run --plan plan.json --dry-run` routes every\n   slice and prints which vendor, model and effort would take it, without\n   creating a worktree or spawning anything. A slice that cannot route is worth\n   knowing about before a worker is spent on its siblings.\n4. **Run it.** `brigadier run --plan plan.json --max-workers 3`. Each slice gets\n   its own git worktree and its own branch; what lands is merged onto one\n   integration branch.\n5. **Review the diffs.** You own the outcome. A worker\'s report is a claim, not\n   evidence.\n\n## What not to do\n\n- Do not edit a file while a slice that owns it is running.\n- Do not do the work yourself because it "looks quick". A one-file change is a\n  one-slice plan, and writing the plan costs less than the context you spend\n  doing it here.\n- Do not invent commands. brigadier has exactly two: `init` and `run`.\n  `brigadier run "fix the parser"` is **not implemented** — a bare task\n  description is refused, and `--plan <file>` (or `--plan -` for stdin) is\n  required.\n\n## Before the first run on a machine\n\n`brigadier init` scans for installed worker CLIs and writes\n`$BRIGADIER_HOME/config.json`, defaulting to `~/.brigadier/config.json`. Without\nit `brigadier run` exits 1 and tells you to run `init`.\n\n## Exit codes worth branching on\n\n`0` succeeded · `1` never started (no config, bad plan file, missing HOME/PATH/USER)\n· `2` usage error · `3` the run started and did not succeed · `130`/`143`\ninterrupted.\n',
    "opencode/README.md":
      "# The opencode surface\n\n**Status: works, with no click and no trust prompt.**\n\nopencode reads `~/.claude/skills/` natively — the same directory Claude Code\nreads — and also reads `~/.agents/skills/`. So the doctrine is already installed\non an opencode machine the moment you run either of:\n\n```sh\nbrigadier install claude-code   # writes ~/.claude/skills/brigadier/\nbrigadier install codex         # writes ~/.agents/skills/brigadier/\n```\n\n`brigadier install opencode` adds the one thing a skill cannot be: a plugin that\nwatches the session and fires the handoff when it runs out of context. It writes\n`plugin/brigadier.js` to `~/.config/opencode/plugin/brigadier.js` and nothing\nelse.\n\n## The unverified line, stated plainly\n\n`plugin/brigadier.js` reacts to the bus events named in `HANDOFF_EVENT_TYPES`,\ncurrently `session.compacted` and `session.compacting`. **Those two names were\nnot verified against a running opencode.** What was verified about opencode is\nnarrower and is about the doctrine rather than the bus: `~/.claude/skills/` and\n`~/.agents/skills/` are both read natively.\n\nIf the hook never fires on your machine, the event is named something else in\nyour build. Read the real name off opencode's debug log and change that one\narray. Everything below it — the sentence, the turn count, the toast — is\nindependent of the name.\n\n## What the plugin does when it fires\n\nWrites one line to stderr, then attempts a toast through `client.tui.showToast`\nif that API exists. The stderr line is the guarantee; the toast is a courtesy and\nits failure is swallowed. The plugin never throws: a plugin that throws takes the\nsession's event loop with it, and a doctrine reminder is not worth anybody's\nsession.\n",
    "opencode/plugin/brigadier.js":
      '/**\n * The brigadier plugin for opencode.\n *\n * opencode reads `~/.claude/skills/` natively, so the doctrine itself arrives\n * with `brigadier install claude-code` and is not duplicated here. What a skill\n * cannot do is watch the session, and that is this file\'s only job: when a\n * session compacts — when it has run out of context — say the one sentence the\n * doctrine exists to say.\n *\n * Zero dependencies. Plain ESM, node: builtins only, no import from the\n * brigadier tree: this file is copied verbatim into\n * `~/.config/opencode/plugin/brigadier.js` and runs on its own.\n */\n\n/**\n * The bus events that mean "this session has run out of context".\n *\n * READ THIS BEFORE FILING A BUG ABOUT THE HOOK NOT FIRING. These two names were\n * NOT verified against a running opencode; opencode\'s event vocabulary is\n * version-dependent, and brigadier\'s verified opencode fact is narrower than\n * people assume — it is that `~/.claude/skills/` and `~/.agents/skills/` are\n * read natively, which is about the doctrine, not about the bus. If your build\n * names the event differently, this array is the one line to change, and\n * `opencode` will log every event type it emits at debug level so you can read\n * the right name off your own machine.\n */\nexport const HANDOFF_EVENT_TYPES = ["session.compacted", "session.compacting"];\n\nconst ADVICE =\n  "Do not carry the remaining work here — write it as a brigadier plan and run `brigadier run --plan <file>`. A fresh worker gets a clean context; you keep the review.";\n\n/** True for exactly the events in `HANDOFF_EVENT_TYPES`, and for nothing else. */\nexport function isHandoffEvent(event) {\n  if (typeof event !== "object" || event === null) {\n    return false;\n  }\n  return HANDOFF_EVENT_TYPES.includes(event.type);\n}\n\n/** The sentence. `turns` is null when the turn count is not honestly known. */\nexport function buildHandoffMessage(turns) {\n  return typeof turns === "number" && Number.isSafeInteger(turns) && turns >= 0\n    ? `brigadier: this session is out of context after ${turns} assistant turn(s). ${ADVICE}`\n    : `brigadier: this session is out of context. ${ADVICE}`;\n}\n\n/**\n * Reads a turn count off an event without believing anything about its shape.\n * Null whenever the count cannot be established, which is most of the time and\n * is fine: the shorter sentence says the same thing.\n */\nexport function readTurnCount(event) {\n  const properties = event?.properties;\n  if (typeof properties !== "object" || properties === null) {\n    return null;\n  }\n  const candidate = properties.messageCount ?? properties.turns;\n  return typeof candidate === "number" && Number.isSafeInteger(candidate)\n    ? candidate\n    : null;\n}\n\nexport const BrigadierPlugin = async ({ client }) => {\n  return {\n    /**\n     * Never throws. A plugin that throws takes the session\'s event loop with\n     * it, and a doctrine reminder is not worth anybody\'s session.\n     */\n    event: async ({ event }) => {\n      if (!isHandoffEvent(event)) {\n        return;\n      }\n      const message = buildHandoffMessage(readTurnCount(event));\n      process.stderr.write(`${message}\\n`);\n      try {\n        await client?.tui?.showToast?.({\n          body: { message, variant: "warning" },\n        });\n      } catch {\n        // The toast is a courtesy. stderr above is the guarantee.\n        return;\n      }\n    },\n  };\n};\n\nexport default BrigadierPlugin;\n',
  });
